# Wuxiang 协作约定

## 当前职责

- 主代理负责架构决策、任务拆分和验收。
- 实现工作委派给 `gpt-5.6-luna`；每次任务分配时必须明确边界、验收条件和文件 ownership。

## 工程规则

- 文档只描述已经决定的范围、可验证的意图和待验证假设；尚未落地的能力要标注为计划，最终 API 以代码为准。
- 涉及代码的改动必须同步考虑安全性、文档同步，以及适当的 test、typecheck、build 检查。
- 不要求每轮额外确认；任务描述已足够明确时直接执行，并在交付时报告文件、验证结果和待核对事项。
- 当前不自动发布、不部署、不提交 commit。所有包暂为 `private`，许可证为 `UNLICENSED`，直到另行决定。
- 只在本次分配的文件范围内修改；发现跨 ownership 冲突时报告给主代理，不覆盖其他代理的工作。

## 验收交付

完成后说明：改动了哪些文件、用什么检查验证、哪些事项仍需主代理或代码代理核对。M0 文档与代码不一致时，以可运行代码和测试结果为准，并回写文档。
